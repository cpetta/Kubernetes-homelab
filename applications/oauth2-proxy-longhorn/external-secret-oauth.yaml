apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: oauth2-proxy-longhorn
  namespace: longhorn-system
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: longhorn-system
    kind: SecretStore
  target:
    name: longhorn-oauth2-proxy # name of created secret
  data:
  - secretKey: client-id # name of created secret key
    remoteRef:
      conversionStrategy: Default
      decodingStrategy: None
      metadataPolicy: None
      key: longhorn-oauth
      property: client_id
  - secretKey: client-secret # name of created secret key
    remoteRef:
      conversionStrategy: Default
      decodingStrategy: None
      metadataPolicy: None
      key: longhorn-oauth
      property: client_secret
  - secretKey: cookie-secret # name of created secret key
    remoteRef:
      conversionStrategy: Default
      decodingStrategy: None
      metadataPolicy: None
      key: longhorn-oauth
      property: client_secret