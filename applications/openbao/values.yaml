global:
  tlsDisable: true

# injector:
#   certs:
#     secretName: vault-certificate

server:
  gateway:
    # tlsRoute:
    #   enabled: true
    #   parentRefs:
    #   - group: gateway.networking.k8s.io
    #     kind: Gateway
    #     name: vault-gateway
    #     namespace: vault
    #   hosts:
    #   - "vault.thegraveshouse.com"
    httpRoute:
      enabled: true
      annotations:
        gethomepage.dev/enabled: "true"
        gethomepage.dev/name: "Vault"
        gethomepage.dev/description: Vault
        gethomepage.dev/icon: vault.png
        gethomepage.dev/group: Admin
        gethomepage.dev/href: https://vault.thegraveshouse.com
        gethomepage.dev/siteMonitor: https://vault.thegraveshouse.com
      parentRefs:
        - group: gateway.networking.k8s.io
          kind: Gateway
          name: traefik-gateway
          namespace: traefik
      hosts:
        - "vault.thegraveshouse.com"
  # service:
  #   dataStorage:
  #     storageClass: "longhorn"
  ha:
    enabled: false
    replicas: 3
ui:
  enabled: true