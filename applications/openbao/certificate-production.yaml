apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vault-certificate-prod
  namespace: vault
spec:
  dnsNames:
  - vault.thegraveshouse.com
  issuerRef:
    kind: ClusterIssuer
    name: cloudflare
  secretName: vault-certificate