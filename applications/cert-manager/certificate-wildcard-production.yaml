apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-cert
  namespace: traefik
spec:
  commonName: '*.thegraveshouse.com'
  dnsNames:
  - thegraveshouse.com
  - '*.thegraveshouse.com'
  issuerRef:
    kind: Issuer
    name: cloudflare-prod
  secretName: wildcard-cert